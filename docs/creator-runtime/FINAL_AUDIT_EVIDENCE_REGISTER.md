# Final Audit Evidence Register

This register is intentionally incomplete until the migration is complete. A blank evidence field is a failed release gate, not an implicit approval.

| Capability domain | Stable source | Tier | Import evidence | Behavior test | Recovery/native provenance | Status |
|---|---|---|---|---|---|---|
| Legacy views 0–48 | `ViewBean`, `ViewBeans` | **R1 only** via `CreatorLegacyViewCapabilityMatrix` and `CreatorRuntimeWidgetCatalog` | `CreatorLegacyViewImporter` maps every inventoried type to a typed runtime widget | Exhaustive 49-type import regression, matrix tests, and full JVM suite pass. A new API 30 AOSP test image and AVD were provisioned, but software emulation had no KVM, reported less than the 4 GiB suggested RAM, never exposed Package Manager readiness, and shut down before application interaction could begin. | No native fallback is permitted; unsupported rendering is blocked as R0 | **Open** — device behavior parity is not yet evidenced |
| Block and event models | `BlockBean`, `EventBean` | **R1 subset / visible R0 remainder** | `CreatorLegacyArtifactImporter` imports canonical state, widget-property, list/map mutations including `setListMap`, toast, vibration, intent/navigation, dialog/media, timer, bounded `repeat`, canonical `forever`/`break`, boolean `if`/`ifElse`, Calendar/SharedPreferences/RequestNetwork, FileUtil, camera/file picker, gyroscope/location, TTS/STT, Bluetooth, MediaPlayer, Date/Time picker, ObjectAnimator, Firebase Auth/Storage/Database, activity, and component events into typed runtime blocks. The evaluator runs nested `@blockId` reporter data for canonical list/map access, sizes, containment, map values, logical/string/math/media/file families; it never generates Java. `repeat` and `forever` cap at 10,000 iterations, while `break` exits the closest typed loop. | Importer regressions cover linked chains, canonical opcodes, timer callback substacks, typed state/widget/collection operations including `setListMap`, FileUtil, Bluetooth, Firebase, intent/dialog/media/picker/calendar/SharedPreferences/RequestNetwork blocks, `forever` with `break`, and rejection of `executeJava`. Executor tests cover list/map reporter evaluation with real legacy argument order, list-of-map mutation, state/collection updates, boolean branching, bounded loop behavior, nested reporters, typed service-result state output, and stateful service arguments; network tests cover URL validation, typed response events, parameters, headers, response headers, and tags. | Unsupported opcodes remain visible R0 and are never delegated to code generation. | **Open** — remaining reporter families, remaining Firebase/network capabilities, and device behavior including Bluetooth transport still require evidence. |
| Component models | `ComponentBean` | **R1 only**; all 30 types map through `CreatorRuntimeComponentServiceMatrix` | Component descriptors persist in `legacy.components` and bind to reviewed runtime service IDs; the runtime dispatcher also registers the reviewed non-component `file` service for legacy FileUtil operations | Component matrix, dispatcher, calendar output, in-memory storage get/set/remove, timer scheduling/cancellation, HTTP request validation/response events, notification permission-gating, path-restricted file operations, ObjectAnimator configuration import, plugin-prefix rejection, and service compilation tests pass | R2/R3 and the host-plugin layer are removed from the active runtime path | **Open** — device/configuration behavior tests remain incomplete for hardware, Firebase, and Ads |
| Project/library/resource models | `ProjectFileBean`, `ProjectLibraryBean`, `ProjectResourceBean`, legacy `files/resource/values{variant}` XML | **R1 metadata subset / visible R0 remainder** | `CreatorLegacyArtifactImporter` persists project file, enabled integration library, image metadata, explicit sound source/timing metadata, named font source metadata, structured `strings.xml`, `colors.xml`, `styles.xml`, `themes.xml`, and `arrays.xml` entries by legacy `values{variant}` qualifier, and typed screen/activity/layout/drawer relationships from `ProjectFileBean` in the versioned Project IR; raw XML source is retained for edit fidelity, while `CreatorRuntimeResourceResolver` resolves path-safe project image files for live image widgets | Importer regressions cover activity runtime kind, class/source/layout names, toolbar/fullscreen/FAB/drawer flags, generated drawer identifiers/layout artifact relationships, Firebase-library configuration, image/resource identity, sound identity/timing, TTF/OTF font identity, all five editable value-resource XML families including a `values-night` qualifier, explicit blocking of arbitrary local/native libraries, and path-safe existing/missing/traversal image resolution; focused and full JVM suites pass | Arbitrary libraries remain visible R0 because they cannot execute as trusted runtime services | **Open** — import evidence for value resources and primary activity/drawer relationships now exists, but live style/color/string application, remaining resource families, non-activity project-file behavior, and device-level behavior remain incomplete |
| Build/export paths | Compiler and `ProjectBuilder` families | Not accepted as a runtime execution tier | `CreatorNativeBuildAdapter`, `CreatorNativeBuildQueue`, and their test were removed | `NO_FALLBACK_SOURCE_AUDIT.md` records no active Creator Runtime plugin classes, plugin references, R2/R3 tier markers, or native-build execution markers; ordinary default-value uses of the word “fallback” are excluded | Creator Runtime live execution must not install a rebuilt APK | **Open** — portable resource import and end-to-end live-project audit remain incomplete |
| Editor and AI actions | Tool registry and editor actions | Runtime operation pipeline | AI `creator_runtime` schema maps screen/widget/property/entry/state/event/revision actions to the same `CreatorRuntimeOperationMapper` and validator as the live editor; JSON arrays now remain typed runtime collections | User/AI pipeline, state-operation, nested conditional, and typed collection-mapping regressions pass; the full JVM suite passes | Revision history and shake recovery remain transparent and host-protected | **Open** — full legacy editor-action import inventory remains incomplete |

## Release gate

The 100% declaration is permitted only when every `Open` row has been decomposed into capability-level records with passing import and behavior evidence. The final audit must contain **zero R0 entries** and **zero R2/R3 execution paths**. An unsupported item must remain a visible R0 exception; it cannot be omitted from the denominator.

## Incremental evidence

`CreatorRuntimeComponentServiceMatrixTest` verifies that all 30 `ComponentBean` identifiers bind to an explicit R1 runtime service. `CreatorLegacyViewCapabilityMatrixTest` verifies that every inventoried legacy view type resolves to R1, and `CreatorLegacyViewImporterTest` imports all 49 view types into the typed runtime document. `CreatorLegacyArtifactImporterTest` now demonstrates canonical direct opcode conversion, stateful Calendar mutation blocks, named SharedPreferences File-component configuration/set/remove blocks, RequestNetwork parameter/header/start blocks, bounded `repeat` plus canonical `forever`/`break`, boolean `if`/`ifElse`, `setVar`, common widget setter, and list/map mutation assignments with nested reporter expressions, ProgressDialog configuration/show/dismiss, SoundPool create/load/play/stop, Bluetooth RFCOMM connection/paired-device blocks, FileUtil mutations, reporter queries, and list-directory state output, camera/file picker, gyroscope/location, text-to-speech, the speech-recognition lifecycle, DatePicker/TimePicker dialog show blocks, ObjectAnimator configuration, Firebase Auth, Firebase Storage upload/download/delete with progress paths, Firebase Database delete/listen/stop with component base-path preservation, activity/component event bindings, timer callback conversion, structured `strings`, `colors`, `styles`, `themes`, and `arrays` value-resource XML import including qualifier retention, and rejection of executable legacy Java instead of plugin or native-build delegation. `CreatorCalendarServiceTest` verifies per-component calendar state plus unknown-field rejection. `CreatorStorageServiceTest` verifies storage get/set/remove plus named legacy File-component configuration. `CreatorNetworkServiceTest` verifies URL validation, direct response events, and stateful RequestNetwork parameters, headers, response headers, and tags. `CreatorFileServiceTest` verifies writes, reads, copies, moves, lists, recursive deletion, and rejection of a path outside approved runtime roots. `CreatorTimerServiceTest` verifies direct scheduling and cancellation; `CreatorRuntimeExecutorTest` verifies typed state increments, boolean branches, bounded repeat and forever execution, break consumption by the closest typed loop, canonical string/math, MediaPlayer, and FileUtil nested reporter evaluation in conditions/counts/state assignments/live widget properties/collection mutations, service results assigned to typed state, and runtime service argument resolution; and `CreatorRuntimeOperationMapperTest` verifies AI arrays remain typed collections. `CreatorProjectDocumentCodecTest` verifies imported service descriptors and typed event blocks survive document serialization. The complete Creator Runtime JVM suite now contains **102 passing tests**. These are JVM-level evidence only; they do **not** close the device behavior, project/library/resource, or legacy build-removal gates.

**Superseding JVM suite count:** After canonical `setListMap`, `mapGetAllKeys`, `strToMap`, `strToListMap`, extended list/map reporter and set-at support, Calendar get-time/format/diff reporter support, Firebase push-key, Firebase Auth status/UID, ObjectAnimator-running, Text-to-Speech-speaking, SharedPreferences `fileGetData`, Intent `intentGetString`, and typed `setTypeface` support, typed widget/state, WebView URL, CalendarView date, `getResStr`, random, and map/list JSON reporter support, explicit image/video resource-family import, imported TextBean font/style/resource-color metadata, and qualifier-aware string/color resolution in the live renderer were added, the complete Creator Runtime JVM suite contains **128 passing tests and zero failures**. The report-specific count above is retained as the prior control-flow milestone; this latest count supersedes it for release-gate evaluation.

`CreatorCalendarServiceTest` additionally verifies that the allow-listed `get_time` query preserves a component-scoped Calendar timestamp. `CreatorRuntimeExecutorTest` verifies `calendarGetTime`, `calendarFormat`, and `calendarDiff` through that runtime-native service path; formatting follows the canonical default `yyyy/MM/dd hh:mm:ss` when the legacy format argument is empty, and `calendarDiff` preserves the legacy first-minus-second timestamp order. These reporter expressions remain typed data and do not invoke generated Java.

`CreatorFirebaseDatabaseService` now exposes an allow-listed `push_key` action, returning `DatabaseReference.push().getKey()` synchronously without writing data. `CreatorRuntimeExecutorTest` verifies that `firebaseGetPushKey` is evaluated through the typed Firebase dispatcher and derives a normalized path from the imported component’s `param1` base-path metadata. The asynchronous `firebaseGetChildren` path is now also runtime-native: `CreatorLegacyArtifactImporter` converts it to `firebase/get_children` with a component-normalized path, result-state ID, and optional typed `children` callback binding; `CreatorFirebaseDatabaseService` collects child maps and publishes the rows; and `CreatorProjectActivity` persists those rows through a validated SYSTEM `STATE_SET` operation before dispatching the callback. The dedicated importer regression and complete **129 passing-test** JVM suite verify the import contract. Live Firebase/device configuration behavior remains open.

The collection evaluator additionally covers canonical `getMapAtPosListmap`, `mapContainValue`, `hashmapGetNumber`, `hashmapGetBoolean`, `hashmapGetMap`, `hashmapListstr`, and `hashmapGetListmap` reporters. `setAtPosListstr`, `setAtPosListnum`, and `setMapAtPosListmap` are converted from their legacy value/index/list argument order into the single typed `set_at` list mutation. Regressions verify returned typed map/list values plus persisted in-place list replacement.

`CreatorLegacyArtifactImporter` now classifies project-resource extensions into explicit `legacy.imageResources` (`png`, `jpg`, `jpeg`, `gif`, `webp`, `bmp`, `svg`) and `legacy.videoResources` (`mp4`, `avi`, `mkv`, `webm`, `3gp`, `mov`) maps in addition to existing sound and font metadata. Importer regressions verify SVG image and WebM video descriptor preservation. Live video-widget behavior, other resource families, and device evidence remain open.

`CreatorRuntimeResourceValues` resolves default-variant `@string/name` and `@color/name` references from the typed imported resource maps. `CreatorProjectActivity` uses that resolver in its live renderer for text, button, input, checkbox, switch, background, text-color, and hint-color properties. Pure JVM coverage verifies resolution and passthrough behavior; qualifier selection beyond the default variant and device-level visual parity remain open.

The resolver now selects an exact imported qualifier such as `values-night` before falling back to `values`. `CreatorProjectActivity` derives the active `-night` qualifier from the Android configuration and supplies it to string/color resolution. JVM regression covers both exact `-night` selection and default fallback; device-level visual validation remains open.

The typed expression evaluator now resolves canonical `getVar`, `getText`, `getEnable`, `getChecked`, transform getters (`alpha`, rotation, translation, scale), `seekbarGetMax`, `seekbarGetProgress`, and `spnGetSelection` from the same runtime widget/state properties used by their setters. `getVar` preserves its legacy literal state-name parameter rather than resolving that name prematurely. JVM regression covers all added getter contracts; visual/widget device behavior remains open.

`objectAnimatorIsRunning` now invokes the existing allow-listed animator `is_running` action as a typed reporter expression. Its regression uses a registered runtime service double and confirms that the result is retained as a Boolean runtime state value; device-level animation timing behavior remains open.

`getResStr` no longer relies on generated `R.string` Java access. The expression evaluator resolves the canonical literal resource name through imported typed `legacy.stringResources`, and its regression verifies that the resolved text becomes a runtime state value. Qualifier-aware runtime UI rendering remains the mechanism for device-specific resource selection.

The evaluator also covers canonical `random` with the legacy inclusive integer range contract. It uses a bounded typed random implementation rather than a host helper or generated Java. JVM regression verifies a returned integral value within the requested inclusive range.

`CreatorTextToSpeechService` now exposes an allow-listed `is_speaking` action backed by the runtime-native Android component. The typed `textToSpeechIsSpeaking` reporter dispatches only to that service and is covered with a deterministic registered-service JVM regression. Device audio behavior remains open for device-level validation.

The evaluator now reads canonical `webViewGetUrl` and `calendarViewGetDate` from typed widget properties (`url` and `date`) maintained by their corresponding runtime setters. Regression verifies both result types as runtime state values; real WebView navigation and CalendarView rendering remain device-level evidence gaps.

Canonical `webViewCanGoBack`, `webViewCanGoForward`, `listGetCheckedPosition`, and `listGetCheckedCount` now use the dedicated read-only `widget` runtime service over Creator Runtime’s active widget registry. The live renderer uses a native `ListView` for legacy list widgets and retains an explicit native choice-mode property, while the service reads actual WebView navigation or ListView checked state rather than generated code or stale document-only values. A deterministic evaluator regression verifies the exact four typed service actions and state results; the complete JVM suite now has **131 passing tests and zero failures**. Real navigation and touch-selection behavior remains device-level evidence still required by the open gates.

Canonical `listSetData`, `listRefresh`, `listSetItemChecked`, `listSmoothScrollTo`, `spnSetData`, `spnRefresh`, `requestFocus`, `webViewGoBack`, and `webViewGoForward` now import as typed allow-listed `widget` runtime actions. Collection-backed list/spinner data is resolved from typed runtime state before service dispatch; the live service updates only registered `ListView`, `Spinner`, `WebView`, or focused views and never evaluates user Java. Importer regression covers action mapping and legacy argument order, while executor regression covers list-state resolution; the complete JVM suite now has **133 passing tests and zero failures**. Native interaction behavior remains device-evidence work under the open audit gates.

Canonical `bluetoothConnectGetRandomUuid`, `bluetoothConnectIsBluetoothActivated`, and `bluetoothConnectIsBluetoothEnabled` now evaluate through `CreatorBluetoothService` only. UUID generation is hardware-independent; status reports a typed activated/enabled pair without invoking generated code, and an unavailable adapter reports both values as false. Deterministic reporter regression verifies UUID and status dispatch/state output; the complete JVM suite now has **134 passing tests and zero failures**. Hardware, permission, and RFCOMM transport behavior remain device-level evidence under the open Bluetooth gate.

Canonical `getLocationX` and `getLocationY` now read `View.getLocationInWindow` through the typed `widget` service, matching the legacy helper’s window-coordinate basis. `spnSetSelection`, WebView cache/history/loading/zoom actions, and CalendarView date/min/max actions are imported as allow-listed live-widget calls, including the legacy `calnedarViewSetMaxDate` spelling. Importer and evaluator regressions verify each typed action and coordinate result; the complete JVM suite now has **136 passing tests and zero failures**. Actual native interaction remains device-evidence work under the open audit gates.

The existing common widget reporters (`getText`, enabled/checked, transforms, SeekBar values, Spinner selection, WebView URL, and CalendarView date) now query the registered native widget first through the reviewed runtime `widget` service. Typed document properties remain the controlled runtime-state value only when no view is registered, such as a JVM-only dispatch; there is no generated-code, plugin, or APK fallback. Deterministic regression verifies live values override stale document values; the complete JVM suite now has **137 passing tests and zero failures**. Device-level rendering and interaction remain open evidence requirements.

Canonical `mathGetDip`, `mathGetDisplayWidth`, and `mathGetDisplayHeight` now dispatch through `CreatorDeviceMetricsService`, which uses Android display metrics and `TypedValue.applyDimension` for the same DIP-to-pixel contract as legacy Sketchware. `listGetCheckedPositions` imports as a typed widget query with result-state output, preserving ordered checked positions as a runtime collection. Dedicated importer/evaluator regressions and the no-fallback source scan pass; the complete JVM suite now has **139 passing tests and zero failures**. Real display configuration and ListView touch behavior remain device-level evidence requirements.

Canonical `progressbarSetIndeterminate` and `setColorFilter` now import as typed `widget` service calls. The active runtime service updates only native `ProgressBar` and `ImageView` instances, using the legacy multiply blend mode for valid Android color values; it rejects unsupported values without generated Java. Importer regression and the complete JVM suite now have **140 passing tests and zero failures**. Device-level visual parity remains explicitly open.

Canonical `dialogOkButton`, `dialogCancelButton`, and `dialogNeutralButton` now configure runtime-native dialog buttons. Their legacy substacks become direct typed `button` bindings; dialog service events carry only a validated callback target and dispatch through the existing runtime executor, never a generated Java listener. Importer regression verifies label, callback target, and block execution binding; the complete JVM suite now has **141 passing tests and zero failures**. Native dialog interaction remains device-level evidence under the open gates.

Canonical `firebaseAdd` and `firebasePush` now import as typed Firebase `update` and `push_update` calls. Map payloads resolve from typed runtime state, while paths are normalized from the Firebase component’s configured base path; the native Firebase service performs `updateChildren` at either the child reference or a freshly pushed child. Importer and executor regressions verify path and map contracts; the complete JVM suite now has **143 passing tests and zero failures**. Firebase configuration, network, and listener behavior remain device-level evidence under the open component gate.

Canonical `firebaseAuthGetCurrentUser` now evaluates the signed-in user’s email through the existing typed Firebase Auth status service, alongside `firebaseAuthIsLoggedIn` and `firebaseAuthGetUid`. The regression proves all three values derive from the reviewed auth status contract rather than generated Firebase calls; the complete JVM suite remains at **143 passing tests and zero failures**. Auth configuration and real-account behavior remain device-level evidence under the open component gate.

Canonical `insertMapToList` and `getMapInList` now preserve legacy argument order in typed IR: map/index/list for insertion and index/list/destination-map for retrieval. The executor evaluates map-state values during insertion and retrieves the selected map through the canonical typed list-map reporter; importer and execution regressions pass. The complete JVM suite now has **145 passing tests and zero failures**. Broader legacy collection behavior remains covered by the still-open block/event gate.

Canonical bitmap file transforms now execute through `CreatorBitmapService` within the same approved storage roots and permission model as `CreatorFileService`. The runtime-native service covers retain-ratio/square/circle/rounded/crop transforms, rotation, scale, skew, color, brightness, contrast, and JPEG EXIF orientation; importer regression verifies all legacy action mappings including the crop width/height order, while executor regression verifies `getJpegRotate`. The complete JVM suite now has **147 passing tests and zero failures**. Actual bitmap rendering and storage-permission behavior remain device-level evidence under the open gates.

Canonical `viewOnClick` now installs a typed dynamic `click` binding instead of generating an Android listener. The live renderer honors installed click bindings on registered native widgets, and the executor regression verifies that installation and subsequent target dispatch remain deferred typed runtime behavior. The complete JVM suite now has **149 passing tests and zero failures**. End-user interaction behavior remains device-level evidence under the open block/event and view gates.

Canonical `setThumbResource` and `setTrackResource` now import as typed SeekBar widget properties. The live renderer resolves imported project drawables or available app drawables at runtime, normalizing legacy `.9` resource names without requiring compiled project resources; the importer regression verifies both mappings. The complete JVM suite now has **150 passing tests and zero failures**. Actual drawable rendering remains device-level evidence under the open view/resource gates.

Canonical custom-adapter data blocks for ListView/Recycler, Spinner, ViewPager, and Grid now persist a typed list-map state reference on their runtime widgets. The live renderer resolves that state after each render and presents map rows through the native widget families, without generated adapter classes; importer regression verifies all five canonical opcodes. The complete JVM suite now has **151 passing tests and zero failures**. Detailed custom row-layout and device interaction parity remain evidence under the open block/event and view gates.

Canonical `adViewLoadAd`, `interstitialadCreate`, `interstitialadLoadAd`, and `interstitialadShow` now map to runtime-native live AdView and component-isolated interstitial services. Interstitial unit IDs are resolved from visible component descriptors; the importer regression verifies banner plus create/load/show contracts. The complete JVM suite now has **152 passing tests and zero failures**. Network delivery, policy configuration, and full-screen ad behavior remain device-level evidence under the open component gate.

Legacy MapView now imports as a native runtime map widget backed by the declared Google Maps Android SDK. The allow-listed `CreatorMapService` owns async map readiness, camera controls, map type, zoom, and the full legacy marker action family; the renderer owns MapView lifecycle and registers the same runtime service instance. Importer regressions cover all canonical map actions plus MapView type conversion. The complete JVM suite now has **154 passing tests and zero failures**. API-key provisioning, network tile delivery, and physical-device rendering remain explicitly open device/configuration evidence.

Legacy activity drawers now render as a native AndroidX `DrawerLayout` when the imported `ProjectFileBean` activity metadata declares a drawer. The renderer mounts the existing typed main widget tree plus the matching `_drawer_<screenId>` runtime screen where present; the allow-listed `CreatorDrawerService` implements canonical `openDrawer`, `closeDrawer`, and `isDrawerOpen` semantics using `GravityCompat.START`. Importer coverage verifies action mapping and reporter expression preservation, while executor coverage proves `isDrawerOpen` obtains its Boolean only from the typed `drawer/is_open` service. The complete JVM suite now has **156 passing tests and zero failures**. Physical gesture, focus, and drawer-screen visual behavior remain device-level evidence under the open audit gates.

The Creator project activity now mirrors legacy `onBackPressed` drawer precedence: a visible runtime-native drawer closes through `GravityCompat.START`, and only a closed drawer delegates to normal activity back navigation. The complete JVM suite remains **158 passing tests and zero failures**. Gesture and back-button behavior on a physical Android device remain explicit open evidence.

Legacy More Blocks now have a typed runtime import contract: definitions become versioned `legacy.moreBlocks` descriptors plus `legacy_moreblock_<id>` bindings; `definedFunc` calls become a bounded `CUSTOM_FUNCTION_CALL`; and `getArg` resolves only from the active scoped argument frame. An end-to-end regression imports an `echo` More Block, invokes it from a view event, and verifies its typed `getArg` value reaches runtime state without Java generation. The explicit `addSourceDirectly` regression proves arbitrary source remains a visible R0 error and does not execute. The complete JVM suite now has **158 passing tests and zero failures**. Return-value semantics and arbitrary direct-source compatibility remain open R0 work and prevent a 100% declaration.

The More Block contract now additionally recognizes the transparent, non-executable legacy source form `return <literal-or-_argument>;` and converts it to a bounded typed `CUSTOM_FUNCTION_RETURN`. Return flow exits typed conditionals and loops, scoped arguments remain isolated per invocation, and a reporter-style `definedFunc` call receives the typed result. Regression imports `return _name;`, invokes the typed More Block as a reporter, and verifies the `Ada` result reaches runtime state. Arbitrary `addSourceDirectly` text remains rejected as visible R0 and never executes. The complete JVM suite now has **160 passing tests and zero failures**; unsupported arbitrary Java still prevents a 100% declaration.

`CreatorLegacyViewImporter` now preserves complete `LayoutBean` geometry and visual data: asymmetric padding and margins, width/height, content/layout gravity, weight/weightSum, and both resource-backed color and drawable backgrounds. `CreatorProjectActivity` consumes only this typed data to apply Android layout parameters and resolve safe project/app drawables in its native live renderer. Regression verifies every stored property and the complete JVM suite now has **159 passing tests and zero failures**. Pixel-level layout parity remains device-level evidence under the open view/resource gates.

`mapGetAllKeys` is now imported as a typed `replace_map_keys` list mutation. The executor clears the destination list, copies the source map’s ordered key set, and persists it back into runtime state. Importer and executor regressions verify the exact legacy map/list parameter order and resulting key order.

The canonical `mapToStr` and `listMapToStr` reporters now serialize typed map/list data directly through the runtime serialization library. Regression verifies the exact JSON values held in state; this replaces generated Java-expression emission while preserving data-only execution.

`strToMap` and `strToListMap` now import as typed JSON replacement mutations. The executor safely parses map/list-map JSON into state, retaining previous values when input is syntactically invalid rather than escaping through generated code. Importer and executor regressions cover canonical argument order and typed results.

The canonical `fileGetData` reporter now dispatches to the existing allow-listed `local_storage` service using the legacy component ID and key. A deterministic service-double regression verifies its request contract and the typed state result, without generated `SharedPreferences` Java.

`CreatorLegacyViewImporter` now preserves legacy `TextBean` `textFont`, `textType`, and both resource-backed text/hint colors as typed widget properties. Regression verifies font/style identity and canonical `@color/` normalization. `CreatorProjectActivity` now resolves an imported named font through the path-safe project resource resolver, applies the requested Typeface style, and safely falls back to the Android default font if metadata or file loading fails. The complete JVM suite remains **125 passing tests and zero failures**; actual font rendering still requires device-level evidence.

Canonical `setTypeface` now imports as a typed `typeface` widget property with the original font and named style. The live renderer applies this dynamic property over imported defaults, recognizing normal, bold, italic, and bold-italic style names and retaining the safe default-font path when a custom resource is unavailable. Importer regression and the complete **126-test** JVM suite pass; device visual behavior remains an explicit evidence gap.

The canonical `intentGetString` reporter now invokes the existing allow-listed runtime-native Intent service’s `get_string` action. A deterministic service-double regression verifies exact intent-ID/key dispatch and typed state output; the complete JVM suite has **127 passing tests and zero failures**.

The typed evaluator now resolves canonical `firebaseAuthIsLoggedIn` and `firebaseAuthGetUid` through `CreatorFirebaseAuthService`’s existing runtime-native `status` action. Deterministic service-double regression verifies the status request plus Boolean and UID state output; the complete JVM suite has **128 passing tests and zero failures**.

Canonical `getExternalStorageDir`, `getPackageDataDir`, and `getPublicDir` reporters now dispatch only through the allow-listed runtime-native `file` service. The service exposes the app's Android data directory and storage paths without generated Java, and restricts public-directory names to documented `Environment.DIRECTORY_*` values. A deterministic executor regression verifies exact dispatch and path return values; the complete JVM suite now has **130 passing tests and zero failures**. Device storage/scoped-storage behavior remains an explicit open evidence requirement.
